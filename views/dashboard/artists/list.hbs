<h1>All artists <a href="/dashboard/artists/create">➕</a></h1>
<hr>
<table>
    <thead>
        <th>Picture</th>
        <th>Name</th>
        <th>Description</th>
        <th>Band</th>
        <th>Update</th>
        <th>Delete</th>
    </thead>
    <tbody>
        {{#each artists}}
            <tr>
                <td><img src={{this.picture}} alt={{this.name}}></td>
                <td>{{this.name}}</td>
                <td>{{this.description}}</td>
                {{#if isBand}}
                <td>✔️</td>
                {{else}}
                <td>❌</td>
                {{/if}}
                <td><a href="/dashboard/artists/{{this._id}}/update"><i class="fas fa-pen"></i></a> </td>
                <td> <a href="/dashboard/artists/{{this._id}}/delete"><i class="fas fa-trash"></i></a></td>
            </tr>
        {{/each}}
    </tbody>
</table>